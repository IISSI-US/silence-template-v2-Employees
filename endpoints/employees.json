{
  "getAll":{
    "route": "/employees/current",
    "method": "GET",
    "sql": "SELECT * FROM employees WHERE employeeId = $loggedId",
    "auth_required": true,
    "allowed_roles": [
      "*"
    ],
    "description": "Gets the currently logged employee"
  }
}